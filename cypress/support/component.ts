// ***********************************************************
// This file is processed and loaded automatically before your component test files.
// ***********************************************************

import './commands';

// Import global styles if needed
// import '../../app/globals.css';

// Note: Component testing with Next.js requires additional setup.
// For now, we'll focus on E2E tests which work out of the box.
// Component testing can be enabled later by installing @cypress/react
// and configuring the mount command properly.

// Uncomment below once @cypress/react is installed and configured:
// import { mount } from 'cypress/react18';
// declare global {
//   namespace Cypress {
//     interface Chainable {
//       mount: typeof mount;
//     }
//   }
// }
// Cypress.Commands.add('mount', mount);

export {};

